ervices:
  srl-latest-torch:
    build:
      dockerfile: ./dockerfile_latest_torch_gpu
    volumes:
      - ../:/code/
    command: /bin/bash
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    